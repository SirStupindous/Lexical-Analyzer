#include <fstream>

void Lexer()
{
    // file pointer
    fstream fout;
  
    // opens an existing csv file or creates a new file.
    fout.open("reportcard.csv", ios::out | ios::app);
    for(int i = 0;i < n; n++) {
        char = getChar();
        if (char == "\" && )
    }
}